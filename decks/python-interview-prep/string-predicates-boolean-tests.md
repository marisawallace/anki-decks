---
id: "19d42ddb15f6435db7d67c0dece42690"
tags: [string]
---

# Front
String predicates (boolean tests)

# Back
`s.isdigit()`, `s.isalpha()`, `s.isalnum()`, `s.isspace()`, `s.startswith(p)`, `s.endswith(p)` — all return a bool.
