---
id: "dc035286983a4d958d9fd32bd9f337d4"
tags: [stdlib]
---

# Front
Binary search into a sorted list

# Back
`import bisect; i = bisect.bisect_left(a, x)` (leftmost insert point); `bisect.insort(a, x)` inserts keeping sorted.
