---
id: "580c3cae1b95443c955605a8451f22dc"
tags: [stdlib, itertools]
---

# Front
Iterate several sequences as one / flatten one level

# Back
`from itertools import chain; for x in chain(a, b, c):`. `chain.from_iterable(grid)` lazily flattens one level.
