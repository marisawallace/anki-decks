---
id: "aa4fa01ecc0043f6a48f23a7ff754972"
tags: [stdlib, math]
---

# Front
Number toolbox: gcd, integer sqrt, factorial, combinations

# Back
`math.gcd(a, b)`, `math.isqrt(n)` (exact int sqrt, e.g. primality bounds), `math.factorial(n)`, `math.comb(n, k)`, `math.perm(n, k)`.
